/* eslint-disable */
import React from 'react';
import BaseUnzerRedirect from './BaseUnzerRedirect';

export default function UnzerSepaDirectDebit(props) {
  return <BaseUnzerRedirect {...props} paymentTag="unzer-sepa-direct-debit" />;
}
